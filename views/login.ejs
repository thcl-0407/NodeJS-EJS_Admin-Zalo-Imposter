<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <title>Imposter System</title>
    <style>
        * {
            font-family: Helvetica, Arial, Tahoma, sans-serif;
        }

        body {
            background: url("https://imposter-zalo.s3-ap-southeast-1.amazonaws.com/355321.jpg");
            background-repeat: no-repeat;
            background-position: center;
            background-size: cover;
        }

        div.container {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 80vh;
        }

        #txtUsername,
        #txtPassword {
            padding: 8pt 0pt 8pt 0pt;
            margin-bottom: 20pt;
            width: 160pt;
            background-color: transparent;
            border: none;
            border-bottom: 1.5px solid #353535;
            outline: none;
            font-weight: 550;
            color: #353535;
            font-size: 0.9rem;
        }

        #txtUsername::placeholder,
        #txtPassword::placeholder{
            color: #505050;
            font-weight: 550;
        }

        #btnDangNhap {
            font-size: 0.9rem;
            font-weight: 520;
            color: #fff;
            padding: 8pt 20pt 8pt 20pt;
            background-color: #016AFF;
            border-radius: 0.8rem;
            font-weight: 550;
            border: none;
            outline: none;
        }

        #btnDangNhap:hover {
            background-color: #0048ad;
            outline: none;
        }

        #frmDangNhap {
            display: block;
            padding: 10pt 35pt 20pt 30pt;
            text-align: center;
            background: rgba(255, 255, 255, 0.9);
            border-radius: 1rem;
            border: none;
            animation-name: fade;
            animation-duration: 0.5s;
        }

        @keyframes fade {
            0%{
                transform: scale(0.2);
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <form action="/login" method="POST" id="frmDangNhap">
            <h2 style="color: #202020; margin-bottom: 20pt;">Imposter System</h2>
            <i class="fa fa-user"></i><span>&nbsp;&nbsp;</span>
            <input name="username" type="text" id="txtUsername" placeholder="Tên đăng nhập" required>
            <br />
            <i class="fa fa-unlock-alt"></i><span>&nbsp;&nbsp;</span>
            <input name="password" type="password" id="txtPassword" placeholder="Mật khẩu" required>
            <br />
            <button type="submit" id="btnDangNhap" style="margin-top: 10pt; margin-bottom: 10pt;">Đăng Nhập</button>
        </form>
    </div>
</body>
</html>